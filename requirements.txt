# 核心依赖 (跨平台兼容)
openai>=1.91.0
litellm>=1.73.0
torch>=2.0.0
transformers>=4.40.0
networkx>=3.0
pydantic>=2.0.0
python_igraph>=0.11.0
tenacity>=8.0.0
tiktoken>=0.7.0
nest_asyncio
numpy
scipy
tqdm
einops
boto3
pyarrow>=14.0.0

# 可选依赖 - vllm (仅 Linux 支持)
# 注意: vllm 在 Windows 上不支持，请勿在 Windows 环境下安装
# vllm>=0.6.0  # 仅 Linux

# 可选依赖 - gritlm (用于嵌入模型)
# gritlm>=1.0.0

# Vector Database backends (optional - install as needed)
# FAISS - efficient local vector search
# faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # For GPU support

# Milvus - distributed vector database
# pymilvus>=2.3.0

# Chroma - lightweight embedded vector database
# chromadb>=0.4.0
